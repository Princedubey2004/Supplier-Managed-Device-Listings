{
    "name": "tortoise-app",
    "version": "1.0.0",
    "description": "Tortoise Device Leasing System",
    "scripts": {
        "install:all": "npm install && cd server && npm install && cd ../client && npm install",
        "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
        "build": "cd client && npm install && npm run build && cd .. && rm -rf public && mv client/dist public",
        "db:migrate": "cd server && npx prisma migrate dev --name init",
        "postinstall": "npx prisma generate --schema=./server/prisma/schema.prisma"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0",
        "bcryptjs": "^3.0.3",
        "cors": "^2.8.5",
        "csv-parser": "^3.2.0",
        "dotenv": "^17.2.3",
        "express": "^5.2.1",
        "jsonwebtoken": "^9.0.3",
        "multer": "^2.0.2",
        "prisma": "^5.22.0"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}